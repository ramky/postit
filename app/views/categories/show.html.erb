<% breadcrum = link_to('All Posts ', posts_path) + "&raquo; ".html_safe + link_to(@category.name,edit_category_path) %>
<%= render 'shared/content_title', title: breadcrum %>

<% @category.posts.each do |post| %>
	<%= render 'posts/post', post: post %>
<% end %>	