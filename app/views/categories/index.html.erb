<% @categories.each do |category| %>
	<div class="row">
		  <div class="span8 well">
		  	<p>
					<%= link_to category.name, edit_category_path(category) %>
				</p>
		  </div>
	</div>
<% end %>			  